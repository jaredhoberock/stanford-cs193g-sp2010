#summary More on CUDA __global__ functions.
#labels Phase-Deploy

= Global Functions =

In the last section, we learned that `__global__` functions serve as the point of entry into a kernel which executes in parallel on a GPU device.  In fact, `__global__` functions expose a particular form of computation called [http://en.wikipedia.org/wiki/Data_parallelism _data parallelism_].